## AGENTS.md

**This workspace is uninitialized — no source code, packages, or build files exist.**

- The repository is in a .NET devcontainer (dotnet 10.0). It has no solution (`.sln`), project (`.csproj`), or source files.
- Dev tools pre-installed: dotnet, node/npm, python/pip, powershell, opencode, claude CLI.
- If asked to write code, run tests, or start building: warn that the project is empty and recommend `/init` to scaffold.
- Formatting conventions in `.editorconfig` (2-space indent for JSON/YAML, 4-space for shell/C#, LF line endings, trailing newline).
- `CLAUDE.md` exists and delegates here.
