# Pin to a specific SDK image tag matching src/global.json, not just ":latest",
# so the container always matches the SDK version CI builds against.
FROM mcr.microsoft.com/devcontainers/dotnet:1-9.0

WORKDIR /workspace
